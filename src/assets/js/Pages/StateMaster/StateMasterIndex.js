var app = angular.module('StateIndex', ['angularUtils.directives.dirPagination']);
var IndexDetails = [];
app.controller('StateMaster', function ($scope, $http, $window) {
    $scope.IndexDetails = Details;
})
